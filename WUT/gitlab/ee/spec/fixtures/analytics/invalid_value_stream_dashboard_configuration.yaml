
title: 'Invalid VSD'

panels:
  - title: 'Invalid panel'
    visualization: 'not_exists'
    gridAttributes:
      yPos: 1
      xPos: 0
      width: 12 # Uses max width otherwise some elements can be hidden and fail the spec.
      height: 12
    data:
      namespace: vsd-test-group/vsd-project
