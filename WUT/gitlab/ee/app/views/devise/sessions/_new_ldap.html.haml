- provider = server['provider_name']

- unless smartcard_enabled_for_ldap?(provider, required: true)
  = render_ce('devise/sessions/new_ldap', server: server, admin_mode: admin_mode)
  - if smartcard_enabled_for_ldap?(provider)
    %hr

= render 'devise/sessions/new_smartcard_ldap', server: server
