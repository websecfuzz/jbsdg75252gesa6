- if credential.expires?
  - expiry_date = credential.expires_at.to_date
  - date_classes = 'gl-flex gl-items-center has-tooltip gl-w-full gl-justify-end md:gl-justify-start'

  - if credential.expired?
    %span{ class: date_classes + ' gl-text-danger', title: _('This credential has expired'), data: { testid: 'expiry-date-icon' } }
      = sprite_icon('error', css_class: "gl-icon gl-mr-2")
      = expiry_date
  - elsif credential.expires_soon?
    %span{ class: date_classes + ' gl-text-warning', data: { testid: 'expiry-date-icon' } }
      = sprite_icon('warning', css_class: "gl-icon gl-mr-2")
      = expiry_date
  - else
    = expiry_date
- else
  = _('Never')
