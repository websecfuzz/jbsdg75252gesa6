query mappedAgents($organizationId: OrganizationsOrganizationID!) {
  organization(id: $organizationId) {
    id
    mappedAgents: workspacesClusterAgents(filter: DIRECTLY_MAPPED) {
      nodes {
        id
      }
    }
  }
}
