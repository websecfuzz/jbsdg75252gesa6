#import "ee/repository/queries/path_locks.fragment.graphql"

query getProjectInfoEE($projectPath: ID!) {
  project(fullPath: $projectPath) {
    id
    userPermissions {
      pushCode
      downloadCode
      createMergeRequestIn
      forkProject
      createPathLock
    }
    ...ProjectPathLocksFragment
  }
}
