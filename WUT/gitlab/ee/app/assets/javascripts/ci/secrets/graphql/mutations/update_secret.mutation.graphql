mutation updateSecret(
  $projectPath: ID!
  $name: String!
  $description: String!
  $secret: String
  $branch: String!
  $environment: String!
) {
  projectSecretUpdate(
    input: {
      projectPath: $projectPath
      name: $name
      description: $description
      secret: $secret
      branch: $branch
      environment: $environment
    }
  ) {
    errors
    projectSecret {
      description
      name
    }
  }
}
