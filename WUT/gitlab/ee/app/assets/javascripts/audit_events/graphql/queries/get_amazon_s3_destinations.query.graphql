query listAmazonS3Configuration($fullPath: ID!) {
  group(fullPath: $fullPath) {
    id
    amazonS3Configurations {
      nodes {
        id
        name
        accessKeyXid
        awsRegion
        bucketName
        active
      }
    }
  }
}
