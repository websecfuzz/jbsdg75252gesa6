query getArtifactRegistryRepository($fullPath: ID!) {
  project(fullPath: $fullPath) {
    id
    googleCloudArtifactRegistryRepository {
      projectId
      repository
      artifactRegistryRepositoryUrl
    }
  }
}
