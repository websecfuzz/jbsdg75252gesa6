import { __, s__ } from '~/locale';

export const DEFAULT_GROUP_PATH = __('{group}');
export const DEFAULT_PROJECT_PATH = __('{project}');
export const DEFAULT_SELECTED_LABEL = s__('ProjectsNew|Select');
