#import "../fragments/scan_policy_source.fragment.graphql"
#import "../fragments/policy_scope.fragment.graphql"

query groupPipelineExecutionSchedulePolicies(
  $fullPath: ID!
  $relationship: SecurityPolicyRelationType = INHERITED
) {
  namespace: group(fullPath: $fullPath) {
    id
    pipelineExecutionSchedulePolicies(relationship: $relationship) {
      nodes {
        csp
        name
        yaml
        editPath
        policyBlobFilePath
        enabled
        policyScope {
          ...PolicyScope
        }
        source {
          ...SecurityPolicySource
        }
        updatedAt
      }
    }
  }
}
