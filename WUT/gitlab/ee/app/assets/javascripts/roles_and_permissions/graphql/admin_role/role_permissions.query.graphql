query adminMemberRolePermissions($includeDescription: Boolean = true) {
  memberRolePermissions: adminMemberRolePermissions {
    nodes {
      description @include(if: $includeDescription)
      name
      value
      requirements
    }
  }
}
