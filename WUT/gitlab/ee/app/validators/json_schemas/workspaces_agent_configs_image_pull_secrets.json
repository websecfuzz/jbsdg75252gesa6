{
  "description": "Image pull secrets for Workspaces Agent Configs",
  "type": "array",
  "x-error": {
    "type": "must be an array of hashes"
  },
  "items": {
    "type": "object",
    "required": [
      "name",
      "namespace"
    ],
    "x-error": {
      "required": "must be an array of hashes containing 'name' and 'namespace' attributes of type string",
      "type": "must be an array of hashes containing 'name' and 'namespace' attributes of type string"
    },
    "properties": {
      "name": {
        "type": "string"
      },
      "namespace": {
        "type": "string"
      }
    }
  }
}
