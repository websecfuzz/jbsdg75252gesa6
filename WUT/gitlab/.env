# ---------- GitLab ----------
GITLAB_IMAGE_TAG=latest            # pin to a version for upgrades, e.g. 17.4.1-ce.0
GITLAB_HOSTNAME=gitlab.example.com # container's internal hostname
GITLAB_EXTERNAL_URL=http://gitlab.example.com
HTTP_PORT=8080
HTTPS_PORT=8443
SSH_PORT=2222

# ---------- PostgreSQL ----------
POSTGRES_USER=gitlab
POSTGRES_PASSWORD=change-me-db
POSTGRES_DB=gitlabhq_production

# ---------- Redis ----------
REDIS_PASSWORD=change-me-redis

# ---------- Backups ----------
# How long to keep backups on disk (seconds). 7 days = 604800
BACKUP_KEEP_SECONDS=604800

# ---------- SMTP (optional) ----------
#SMTP_ADDRESS=smtp.example.com
#SMTP_PORT=587
#SMTP_USER=no-reply@example.com
#SMTP_PASSWORD=change-me-smtp
#SMTP_DOMAIN=example.com
#SMTP_FROM=no-reply@example.com

# ---------- Registry (optional) ----------
#REGISTRY_EXTERNAL_URL=https://registry.example.com

