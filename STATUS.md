# Artifact Status

**Badges applied for:**

1. **Artifacts Evaluated - Functional**
   - The artifacts associated with the research are documented, consistent, complete, exercisable, and include appropriate evidence of verification and validation. Our fuzzer, SQLiFuzz, is provided with scripts (`/scripts`) to fully automate the Docker setup and fuzzing process for the Web applications Under Test (WUT).

2. **Artifacts Evaluated - Reusable**
   - The artifacts are of a quality that significantly exceeds minimal functionality. The repository is well-structured, distinguishing between the core logic (`/crawler`, `/sqlifuzz`), the target web apps (`/WUT`), and execution scripts (`/scripts`), which strongly facilitates reuse and repurposing by other researchers against new web applications.

3. **Artifacts Available**
   - The artifact is publicly available at https://github.com/websecfuzz/SQLiFuzz. The current submission is the complete source artifact.
